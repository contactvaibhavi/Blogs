<main>

	<% if user_signed_in? %>
	<div id="new-post-form-container">
		<%= form_for @post, remote: true do |f| %>
			<%= f.label :content, "Add Post" %>
			<%= f.text_area :content, placeholder: "Type Here" %>
			<%= f.submit %>
		<% end %>
	</div>
	<% end %>
<p>hvhfjjujj</p>
	<div id="posts-list-container">
		<ul id="posts-list">
			<% @posts.each, remote: true do |post| %>
				<%= render 'post', p: post %>
			<% end %>
		</ul>
	</div>


</main>